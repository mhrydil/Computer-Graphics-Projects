#include <stdio.h>
#include <math.h>
#include "matLib.h"



void fillEdges(vec4* vert, int size); //creates the vertices that make up the
	//edges and base of the cone

void fillColors(vec4* colors, int size); //Fills all of the triangles with
	// random colors
